# prisma

`schema.prisma` + migrations arrive in Phase 2 (Company, User, Department, Route, Invite, Case, CaseEvent, Idea, Problem, Initiative - see docs/DATA_MODEL.md).
`seed/` ports the demo rows from legacy/demo/js/data.js into one demo company.

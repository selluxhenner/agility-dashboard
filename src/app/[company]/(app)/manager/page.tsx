// MANAGER home. Decisions waiting, wait ledger (median to first answer, share within the 14-day promise), four stall reasons, movement since baseline.
export const metadata = { title: "Overview" };
export default function ManagerOverviewPage() {
  return (
    <>
      <h1>Overview</h1>
      <p className="nh-hint">MANAGER home. Decisions waiting, wait ledger (median to first answer, share within the 14-day promise), four stall reasons, movement since baseline.</p>
    </>
  );
}

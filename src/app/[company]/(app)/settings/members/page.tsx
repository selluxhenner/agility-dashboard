// Invite users, assign roles (manager / leader / member), deactivate.
export const metadata = { title: "Members" };
export default function MembersSettingsPage() {
  return (
    <>
      <h1>Members</h1>
      <p className="nh-hint">Invite users, assign roles (manager / leader / member), deactivate.</p>
    </>
  );
}

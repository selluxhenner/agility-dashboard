# Copy to .env.local - never commit .env.local (see .gitignore).
# Nothing is read yet; these are the variables Phase 2 introduces.
DATABASE_URL="postgresql://nexthub:nexthub@localhost:5432/nexthub"
AUTH_SECRET="change-me"
AUTH_URL="http://localhost:3000"
# path -> nexthub.app/acme/...   subdomain -> acme.nexthub.app/...
TENANT_MODE="path"

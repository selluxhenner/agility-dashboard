# tests

Node scripts, no framework (same as the demo .github/ci/):
- store.test.cjs - reducer unit tests
- smoke.cjs - every page renders, all three roles, mobile width (needs the site served on :8780)
- flow.test.cjs - landing -> login -> company login -> raise a case -> inbox -> answer -> my cases -> ledger moves (playwright)
